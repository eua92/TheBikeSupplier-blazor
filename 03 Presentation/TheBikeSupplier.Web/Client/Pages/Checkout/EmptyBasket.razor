@inject NavigationManager _navigationManager;

<div class="d-flex flex-column align-items-center">
    <i class="fa fa-shopping-cart fa-3x mt-5 mb-4" />
    <div class="lead mb-3 font-weight-medium">Your basket is empty</div>
    <div class="text-center col-9 col-md-7 col-lg-4 mb-2">Looks like there's nothing in your basket yet. Go ahead and explore our bicycle collection.</div>
    <button class="btn btn-info" @onclick="@(() => _navigationManager.NavigateTo("/bikes"))">View shop</button>
</div>
